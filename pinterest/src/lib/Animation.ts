export const dinnerArr: string[] = [];
for (let i = 1; i < 14; i++) {
  dinnerArr.push(`dinner${i}`);
}

export const HomeArr: string[] = [];
for (let i = 1; i < 14; i++) {
  HomeArr.push(`home${i}`);
}

export const FasionArr: string[] = [];
for (let i = 1; i < 14; i++) {
  FasionArr.push(`fasion${i}`);
}

export const GardenArr: string[] = [];
for (let i = 1; i < 14; i++) {
  GardenArr.push(`garden${i}`);
}
